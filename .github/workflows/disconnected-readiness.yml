name: Disconnected Readiness
on:
  pull_request:
    branches:
      - main
      - master

jobs:
  check:
    uses: AjayJagan/disconnected-readiness-scorer/.github/workflows/disconnected-readiness-check.yml@main
    with:
      rules: ""
