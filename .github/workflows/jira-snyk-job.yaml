name: Create Jira from Snyk

on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * 1'

env:
    # JIRA envs
  JIRA_SERVER: https://issues.redhat.com
  JIRA_PROJECT_ID: 12340620 # refers to RHOAIENG
  JIRA_COMPONENT_NAMES: Platform # A list of comma separated values. Eg. Platform,Dashboard
  JIRA_EPIC_ID: RHOAIENG-1430 # refers to https://issues.redhat.com/browse/RHOAIENG-1430(This has to be periodically changed)
  # SNYK envs
  SNYK_ORG_ID: ed870ef2-8f76-4ea1-ad4b-dacfa225eb69 # refers to the RHOAI org in Snyk
  SNYK_PROJECT_ID: 570fd96f-09ad-488b-bb37-f822c34c973f # refers to the red-hat-data-services/rhods-operator
  #other envs
  DRY_RUN: true # to be removed later

jobs:
  jira-snyk-sync-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pysnyk==0.9.19
          pip install python-dotenv==1.0.0
          pip install jira==3.6.0

      - name: execute py script for automation 
        env:
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          SNYK_API_TOKEN: ${{ secrets.SNYK_API_TOKEN }}
          EXCLUDE_FILES_FILE_PATH: ./.github/workflows/supporting-files/jira-snyk-job/exclude_files.json
        run: curl https://raw.githubusercontent.com/AjayJagan/jira-snyk-automation-scripts/main/jira-automation.py | python - # This script can be hosted in a repo for general use